// All plugins must be created instances of Plugin. The key must match the type of the collected backend data.
export {default as text} from './plugins/text.js';
export {default as calendar} from './plugins/calendar.js';
// Add more plugins here if needed.
